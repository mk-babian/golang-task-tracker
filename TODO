Core requirements:
    Add tasks with descriptions
    List all tasks
    Mark tasks complete
    Delete tasks
    Save/load from JSON

SEPARATE TODOs:

func printHelp(){
    Define each command []
    Make it look good []
}

func createJson(){
    Create JSON file [x]
}

func addTask(){
    Add a new task to an empty file [x]
    Append a task to an existing file (DIFFICULT) []
}

func delTask(){
    Read from existing .json file []
    Delete task(s) from .json file []
}
